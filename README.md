Pour executer le projet en locel et en dev (la prod n'est pas encore implementee) :

1. Cloner le repo
2. Executer `docker compose -f compose.dev.yaml up -d --build`
3. L'application est accessible sur `http://localhost:5173` et le backend sur `http://localhost:5002`